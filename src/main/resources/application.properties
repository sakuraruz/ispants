# server.port=8086
# spring.application.name=pivot-analytics

# # PostgreSQL подключение
# spring.datasource.url=jdbc:postgresql://localhost:5433/pivot_db
# # spring.datasource.username=postgres
# # spring.datasource.password=postgres
# spring.datasource.username=localhost
# spring.datasource.password=!!!2006!!!
# spring.datasource.driver-class-name=org.postgresql.Driver

# # Логирование SQL
# logging.level.org.springframework.jdbc=DEBUG
# logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG

server.port=8091
spring.application.name=pivot-analytics

# PostgreSQL подключение (из docker-compose)
spring.datasource.url=jdbc:postgresql://localhost:5433/analytics_db
spring.datasource.username=root1
spring.datasource.password=rootpassword1
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jdbc.template.query-timeout=30

# Логирование SQL
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG